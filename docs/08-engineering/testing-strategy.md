# Testing Strategy

Testing pyramid includes unit, contract, database/integration, isolation/security, end-to-end and operational verification. Each work package defines evidence.

Phase 0 contract tests validate draft machine-readable contracts before production implementation begins. Contract validation must pass before a draft schema is used as an implementation input.
