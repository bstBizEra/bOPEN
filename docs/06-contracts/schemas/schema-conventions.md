# Schema Conventions

Use stable IDs, semantic versions, explicit nullability, bounded strings, documented enums, no ambiguous timestamps and compatibility tests. Draft schema IDs include `draft`.

JSON schema drafts use a `bopen://` `$id` and top-level `"status": "draft"` marker. YAML examples with draft versions use top-level `status: draft`.
