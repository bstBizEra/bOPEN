# Architecture Principles

Explicit context; deny by default; contract first; database-enforced isolation; modular boundaries; event/audit correlation; independent entitlement; no industry leakage into kernel; reversible decisions where practical.
