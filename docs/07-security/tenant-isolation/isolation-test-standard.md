# Isolation Test Standard

Every tenant-owned data path requires positive and negative tests, direct database enforcement tests, privileged-path tests and evidence that context cannot be client-forged.
