# Module Boundary Rules

- Use public module interfaces and registered events.
- Do not query another module's private tables.
- Update manifest and contracts with behavior.
- Keep platform kernel free of industry-specific logic.
