# Validation Loop (draft)

Run repository validation, contract/schema checks, full tests, negative fixtures,
manifest checks, secret/supply-chain checks and `git diff --check`. Keep command
outputs and exit codes separate. Passing tests are evidence only, never authority.
