# Worktree Loop (draft)

Use one authorized work item per worktree under `../bopen-worktrees/<id>-<slug>`.
Record branch, base/head/tree and custody. Never reuse a dirty worktree, attach a
second branch to a claimed base, or remove a worktree before evidence and closure.
