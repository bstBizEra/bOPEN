---
name: bopen-module-contract
description: Create or review a bOPEN module contract, manifest, boundaries, capabilities, permissions, entitlements, APIs, events, data and operations.
---

# Module Contract

Classify the module layer and owner.
Define dependencies and public interfaces.
Register resources, tenant ownership, permissions and entitlements.
Define API/event versions, migrations, observability and operational states.
Use `templates/module.yaml` and `docs/05-modules/MODULE-CONTRACT.md`.
