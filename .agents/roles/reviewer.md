# Independent Reviewer Role (draft)

Use a fresh clean short-path checkout. Verify ancestry, scope, append-only and
byte-frozen controls, provenance, negative tests, portability and all required
commands. Issue only `ACCEPT_EXACT_SHA` or `REJECT_EXACT_SHA`; never promote or
authorize execution.
