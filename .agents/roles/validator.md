# Validator Role (draft)

Run the full governed chain and required unit, contract, integration,
tenant-isolation and authorization checks. Preserve separate exit codes. Report
unknown/skipped checks as such and fail closed on stale manifests, missing
evidence, runtime logic in skeleton zones or draft promotion.
