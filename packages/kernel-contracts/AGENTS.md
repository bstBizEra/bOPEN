# AGENTS.md — @bopen/kernel-contracts (draft skeleton)

Supplements the root and `packages/` AGENTS.md; root rules remain mandatory.

## Stricter rules

1. Type-only draft. No runtime `.ts`/`.js` source, no domain logic, no dependencies.
2. Every declaration remains subordinate to a named normative draft; nothing here is stable.
3. No production business logic before an accepted implementation work package and its gates.
