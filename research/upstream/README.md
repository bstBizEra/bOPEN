# Upstream Research Clones

Clone pinned upstream repositories here for local study. This directory is git-ignored except for this file and `.gitkeep`.

Do not copy upstream files into bOPEN implementation directories. Record repository, commit, license and environment in `research/sources/` and findings in `research/findings/`.
