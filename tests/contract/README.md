# Contract Test Tier

**Status:** Draft guard scaffold

This tier currently contains no business tests. Its placeholder guard passes only while kernel zones contain no runtime implementation. Once implementation is introduced, this tier requires an explicitly named negative test containing the marker `BOPEN_NEGATIVE_TEST: contract` and fails closed until one exists.
