# AGENTS.md — tests/contract (draft skeleton)

Supplements root and `tests/` AGENTS.md; root rules remain mandatory.

## Stricter rules

1. Guard (negative) tests only until an implementation work package is accepted.
2. Adding an implementation requires arming `negative-tests.manifest.json` and its required negatives.
3. No production business logic or live fixtures.
