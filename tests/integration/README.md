# tests/integration (skeleton tier)

**Status:** draft skeleton; fail-closed guard only.

Placeholder tier for future integration suites. The guard test fails closed if an implementation appears without its armed negative tests. No business tests are present yet.
