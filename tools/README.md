# tools

Repository validation, generation and maintenance tooling.

- `validate_repository.py`: validates mandatory repository governance invariants.
- `validate_contracts.py`: validates draft machine-readable contract metadata and parseability.
